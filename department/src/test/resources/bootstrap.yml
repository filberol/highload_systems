spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  liquibase:
    enabled: true
  datasource:
    driver-class-name: org:postgresql:Driver
    username: test
    password: test