spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: postgres
  liquibase:
    url: jdbc:postgresql://localhost:5432/test
    user: postgres
    password: postgres
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  main:
    allow-circular-references: true

eureka:
  client:
    enabled: false
