spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: test
    password: test
  liquibase:
    url: jdbc:postgresql://localhost:5432/postgres
    user: test
    password: test
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  main:
    allow-circular-references: true

eureka:
  client:
    enabled: false
